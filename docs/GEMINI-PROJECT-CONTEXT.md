# BioCube.ru — Полный контекст проекта для Gemini

## 🎯 О ПРОЕКТЕ

**BioCube** — премиальный сервис умных аквариумов с IoT-мониторингом, автоматическим обслуживанием и профессиональной поддержкой биологов. Сайт — современный маркетинговый лендинг с инновационным дизайном и интерактивными секциями.

**Домен**: `bio-cube.ru` (deployed on Netlify)

**Тип проекта**: SPA лендинг с двумя режимами контента (installation/service)

---

## 🛠 ТЕХНОЛОГИЧЕСКИЙ СТЕК

### Core
- **Framework**: React 18.3.1 + TypeScript 5.8.3
- **Build Tool**: Vite 5.4.19 (with SWC plugin)
- **Routing**: React Router DOM 6.30.1 (SPA)
- **Package Manager**: Bun / npm

### Styling & UI
- **CSS Framework**: Tailwind CSS 3.4.17
- **UI Components**: shadcn/ui (Radix UI primitives)
- **Icons**: Lucide React 0.462.0
- **Animations**: 
  - Framer Motion 12.26.2 (для интерактивных анимаций)
  - tailwindcss-animate (базовые анимации)

### Backend & Data
- **Backend**: Supabase (Edge Functions)
- **State Management**: React Context API
- **Forms**: React Hook Form 7.61.1 + Zod 3.25.76
- **Data Fetching**: TanStack React Query 5.83.0

### Другие библиотеки
- **Date**: date-fns 3.6.0
- **Carousel**: embla-carousel-react 8.6.0
- **Charts**: recharts 2.15.4
- **Notifications**: sonner 1.7.4
- **Drawer**: vaul 0.9.9

---

## 📁 СТРУКТУРА ПРОЕКТА

```
src/
├── pages/
│   ├── Index.tsx              # Главная страница (основной лендинг)
│   ├── Marketplace.tsx        # Маркетплейс (в разработке)
│   ├── NotFound.tsx
│   ├── Offer.tsx              # Договор оферты
│   └── Privacy.tsx            # Политика конфиденциальности
│
├── components/
│   ├── sections/              # Все секции лендинга
│   │   ├── NewHeader.tsx
│   │   ├── NewHeroSection.tsx
│   │   ├── PartnersTrustBar.tsx
│   │   ├── BioSolutions.tsx          # Zigzag продуктовая витрина
│   │   ├── EngineeringCore.tsx       # Exploded view аквариума (NEW)
│   │   ├── IntelligentCore.tsx       # IoT/App секция (тёмная)
│   │   ├── BioScoreDashboard.tsx     # Графики/биометрика (тёмная Future Tech)
│   │   ├── VirtualTryOn.tsx          # AR примерка в интерьер
│   │   ├── ThreePaths.tsx            # 3 пути навигации (светлая)
│   │   ├── MacroWorld.tsx            # Детали экосистемы
│   │   ├── ParticipationLevels.tsx   # Тарифы/цены
│   │   ├── ComparisonTable.tsx       # Сравнение с конкурентами
│   │   ├── TrustBar.tsx              # Бейджи гарантий
│   │   ├── LeadForm.tsx              # Форма заявки
│   │   ├── CTASection.tsx
│   │   └── NewFooter.tsx
│   │
│   ├── ui/                     # shadcn/ui компоненты (49 файлов)
│   │   └── ...
│   │
│   ├── Header.tsx
│   ├── Footer.tsx
│   ├── ContactFormDialog.tsx
│   ├── ModeSelectDialog.tsx
│   └── ...
│
├── contexts/
│   └── ServiceModeContext.tsx  # Управление режимами (installation/service)
│
├── hooks/
│   ├── use-mobile.tsx
│   └── use-toast.ts
│
├── lib/
│   ├── submitLead.ts           # Единый helper для отправки заявок
│   ├── contact.ts              # Контакты (телефоны, email, адрес)
│   ├── phone.ts                # Нормализация телефонов
│   └── utils.ts                # Утилиты (cn, etc.)
│
├── integrations/
│   └── supabase/
│       ├── client.ts
│       └── types.ts
│
├── App.tsx
├── App.css
├── main.tsx
└── index.css                   # Глобальные стили + CSS переменные
```

---

## 🎨 ДИЗАЙН-СИСТЕМА

### Цветовая палитра (Brand Colors)

**Primary (Neon Mint)**:
- `#4DB6AC` — основной акцент
- `#26A69A` — тёмный оттенок
- `#2DD4BF` — светлый оттенок

**Dark Theme Colors** (для "Future Tech" секций):
- Background: `#1A1A1A`, `#0a0f0f`, `#0d1212`
- Cards: `#222222`
- Text: `white`, `gray-400`

**Light Theme Colors** ("Clinical Zen"):
- Background: `#F8FAFC`, `white`, `gray-50`
- Cards: `white` с subtle shadows
- Text: `gray-900`, `gray-600`, `gray-400`

### CSS Переменные (index.css)

```css
:root {
  --primary: 168 60% 50%;        /* #4DB6AC */
  --background: 180 5% 98%;      /* #F8FAFC */
  --foreground: 220 15% 20%;     /* Dark gray text */
  /* ... */
}
```

### Стилистические направления

1. **"Clinical Zen"** — чистый, воздушный стиль:
   - Фон: `#F8FAFC`
   - Много белого пространства
   - Стеклянные карточки (glassmorphism): `bg-white/70 backdrop-blur-md`

2. **"Future Tech"** — тёмный технический стиль:
   - Фон: `#1A1A1A`
   - Neon Mint акценты с glow эффектами
   - Графики с `drop-shadow` для свечения

3. **Переходы**:
   - `DiveTransition` — погружение в темноту (градиент `white → #0a0f0f`)
   - `SurfaceTransition` — выныривание к свету (`#0a0f0f → white`)
   - `SoftLightTransition` — между светлыми секциями
   - `BrandTransition` — к финалу

---

## 📐 СТРУКТУРА ЛЕНДИНГА (Порядок секций)

Текущий порядок секций в `Index.tsx` (сверху вниз):

1. **🌅 СВЕТ**: `NewHeroSection` + `PartnersTrustBar` (Light)
2. **📦 ПРОДУКТ**: `BioSolutions` — Zigzag продуктовая витрина (Light)
3. **🔧 ENGINEERING**: `EngineeringCore` — Exploded View аквариума (Light - Clinical Zen) **[NEW]**
4. **🌊 ПЕРЕХОД**: `DiveTransition` — градиент в темноту
5. **🔬 TECH BLOCK** (Dark):
   - `IntelligentCore` — IoT/App "BioCube OS"
   - `BioScoreDashboard` — Графики/биометрика (Future Tech theme)
6. **☀️ ПЕРЕХОД**: `SurfaceTransition` — градиент к свету
7. **🏠 LIFESTYLE**: `VirtualTryOn` — AR примерка в интерьер (Light - Clinical Zen)
8. **🎯 3 ПУТИ**: `ThreePaths` — навигация (Light)
9. **🔍 ДЕТАЛИ**: `MacroWorld` — детали экосистемы (Light)
10. **💰 PRICING** (Light):
    - `ParticipationLevels` — тарифы
    - `ComparisonTable` — сравнение с конкурентами
    - `TrustBar` — бейджи гарантий
11. **💎 ФИНАЛ**: `LeadForm` + `CTASection` + `NewFooter`

**Концепция**: "Путешествие погружения"
- Начинаем на "поверхности" (светлые секции)
- "Ныряем" в технологии (тёмный Tech Block)
- "Выныриваем" обратно к выбору и покупке

---

## 🔧 КЛЮЧЕВЫЕ КОМПОНЕНТЫ

### EngineeringCore.tsx (NEW)
**Цель**: Показать "анатомию" аквариума в exploded view (взрыв-схема).

**Визуал**:
- 3 вертикально разделённых слоя (floating animation):
  1. **Top**: Smart Lighting Unit (тёмно-серый с LED полоской)
  2. **Middle**: Optiwhite Glass Tank (прозрачный, с водой, растениями, рыбами)
  3. **Bottom**: Filtration & IoT Base (компартменты с фильтрами, помпой, IoT модулем)

**Интерактивность**:
- 4 пульсирующих хотспота (#4DB6AC)
- На hover: появляется glassmorphism карточка с описанием (desktop) или показывается ниже (mobile)
- Desktop: SVG connector lines от хотспота к карточке
- Framer Motion для floating анимации слоёв и появления элементов

**Стиль**: "Clinical Zen" (`bg-[#F8FAFC]`)

**Hotspots**:
1. "Full Spectrum LED" — на верхнем слое
2. "Optiwhite™ Glass" — на среднем слое
3. "Bio-Sensor Array" — на среднем слое
4. "Matrix Purification" — на нижнем слое

### IntelligentCore.tsx
**Цель**: Показать IoT/App функциональность "BioCube OS".

**Визуал**:
- Тёмная тема (`bg-gradient-to-b from-[#0a0f0f] via-[#0d1212] to-[#0a0f0f]`)
- Слева: мобильный телефон (iPhone mockup) с интерфейсом приложения
- Справа: описание функций
- Neon glow эффекты (#4DB6AC)

**Контент**: IoT-сенсоры, push-уведомления, мониторинг 12 параметров воды 24/7

### BioScoreDashboard.tsx
**Цель**: Показать графики и биометрику системы.

**Визуал**:
- Тёмная "Future Tech" тема (`bg-[#1A1A1A]`)
- Круговая диаграмма с процентами (98%)
- Метрики (Чистота воды, Циркадный ритм, Биоактивность)
- Wave charts с Neon Mint strokes (#4DB6AC, #2DD4BF)
- Glow эффекты на графиках (`drop-shadow`)

**Стиль**: Технический, с неоновыми акцентами

### VirtualTryOn.tsx
**Цель**: AR примерка аквариума в интерьер.

**Визуал**:
- Светлая тема ("Clinical Zen")
- Split screen: слева текст + кнопка загрузки, справа before/after slider
- Drag-to-reveal эффект (показывает "до" и "после")

### ThreePaths.tsx
**Цель**: Навигация по 3 путям (Конструктор, 3D-Примерка, Сервис).

**Визуал**:
- Светлая тема (после переделки)
- 3 карточки с градиентными иконками
- Hover эффекты с glow

### ParticipationLevels.tsx
**Цель**: Показать тарифы.

**Контент**:
1. **Эксперт (Support)** — 1 990₽/мес
2. **Хоббист (Guided)** — 4 990₽/мес ⭐ популярный
3. **Наблюдатель (Full Service)** — 12 990₽/мес

### ComparisonTable.tsx
**Цель**: Сравнение BioCube vs Обычный магазин.

**Таблица**: Гарантия на рыб, Фиксированная цена, Цифровые отчёты, Страховка от протечек

---

## 🔄 РЕЖИМЫ КОНТЕНТА (ServiceModeContext)

Проект поддерживает 2 режима:

1. **`installation`** — установка аквариума "под ключ" (по умолчанию)
2. **`service`** — обслуживание / спасение / диагностика

**Реализация**:
- Query param: `?mode=installation` или `?mode=service`
- Контекст `ServiceModeContext` синхронизирует с URL
- В зависимости от режима могут меняться секции (сейчас основной режим — `installation`)

**Использование**:
```typescript
import { useServiceMode } from "@/contexts/ServiceModeContext";
const { mode } = useServiceMode(); // 'installation' | 'service'
```

---

## 📝 ФОРМЫ И ЗАЯВКИ

### Единый helper: `submitLead()`

Все формы используют единый helper из `src/lib/submitLead.ts`:

```typescript
import { submitLead } from "@/lib/submitLead";

const result = await submitLead(
  { name, phone, comment, messenger, honeypot },
  { mode, formName }
);
```

**Функционал**:
- ✅ Honeypot защита от ботов
- ✅ Rate limit (60 сек между отправками)
- ✅ Валидация телефонов (нормализация через `normalizeRuPhoneForCRM`)
- ✅ UTM-параметры из URL
- ✅ Отправка в Supabase Edge Function `lead-webhook`
- ✅ GTM события (`lead_submit_success`)

**Backend**:
- Netlify Function: `netlify/functions/lead-webhook.js` → проксирует в Supabase
- Supabase Edge Function: `supabase/functions/lead-webhook/` → отправляет в Albato/CRM
- URL вебхука хранится в Supabase Secrets (`ALBATO_WEBHOOK_URL`)

---

## 📞 КОНТАКТЫ

Все контакты в `src/lib/contact.ts`:

```typescript
CONTACT_PHONE_DISPLAY = "+7 (967) 133-07-73"
CONTACT_PHONE_E164 = "+79671330773"
WHATSAPP_PHONE_DISPLAY = "+7 (916) 433-30-70"
WHATSAPP_LINK = "https://wa.me/79164333070"
TELEGRAM_LINK = "https://t.me/biocube_zabota_bot"
CONTACT_EMAIL = "info@bio-cube.ru"
CONTACT_ADDRESS = "Москва"
```

---

## 🚀 DEPLOYMENT

### Netlify
- **Build command**: `npm run build`
- **Publish directory**: `dist`
- **Environment variables**: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`
- **Functions**: `netlify/functions/` (Edge Functions)

### Supabase
- Edge Functions: `supabase/functions/lead-webhook/`
- Secrets: `ALBATO_WEBHOOK_URL` (URL вебхука для Albato/CRM)

---

## 📚 ДОКУМЕНТАЦИЯ

Все в `docs/`:

- `ARCHITECTURE.md` — архитектура проекта
- `CHANGELOG.md` — история изменений
- `DEPLOY.md`, `DEPLOY-BEGET.md` — инструкции по деплою
- `FORMS.md` — формы → CRM + дебаг
- `RUNBOOK.md` — операционные процедуры
- `CONTENT.md` — где и как менять контент
- `STRUCTURE.md` — структура проекта

---

## 🎯 ТЕКУЩИЕ ЗАДАЧИ И КОНТЕКСТ

### Недавно добавлено:
- ✅ **EngineeringCore.tsx** — Exploded View аквариума в разрезе
  - Нужно заменить placeholder-визуализацию на реальное изображение (3D render или фото)
  - Изображение должно показывать аквариум с водой, растениями, декорациями, рыбами в разрезе

### Стилистические требования:
- Использовать только Tailwind CSS классы
- Framer Motion для анимаций
- Responsive design (mobile-first подход)
- Следовать дизайн-системе: "Clinical Zen" для светлых секций, "Future Tech" для тёмных

### Важно:
- **Не удалять контент** — только перемещать и переставлять секции
- **Сохранять все функциональности** — формы, анимации, интерактивность
- **Следовать цветовой палитре** — #4DB6AC (Neon Mint) как основной акцент

---

## 🔑 КЛЮЧЕВЫЕ ФИЧИ ПРОДУКТА

### BioCube — это:
- **IoT-сенсоры**: pH, температура, жёсткость воды в реальном времени
- **BioCube OS**: мобильное приложение для мониторинга
- **Push-уведомления**: владельцу и биологам
- **Страховка**: от протечек (1 млн ₽)
- **Гарантия**: 100% на живых обитателей
- **SLA**: 15 минут на критические проблемы
- **Профессиональное обслуживание**: биологами 24/7

### Тарифы:
1. **Эксперт** — 1 990₽/мес (только расходники)
2. **Хоббист** — 4 990₽/мес (ежемесячное обслуживание, SLA 24ч)
3. **Наблюдатель** — 12 990₽/мес (полное обслуживание, SLA 4ч)

---

## 💡 КОНВЕНЦИИ КОДА

### Именование:
- Компоненты: `PascalCase` (например, `EngineeringCore.tsx`)
- Функции: `camelCase` (например, `submitLead`)
- Константы: `UPPER_SNAKE_CASE` (например, `CONTACT_PHONE_E164`)
- CSS классы: Tailwind utilities

### Импорты:
- Абсолютные пути: `@/components/...`, `@/lib/...`
- shadcn/ui: `@/components/ui/...`

### TypeScript:
- Строгие типы для всех функций
- Интерфейсы для props и данных
- `export type` для типов, используемых в других модулях

---

## 🎨 АНИМАЦИИ

### Framer Motion:
- Используется для сложных интерактивных анимаций
- `useInView` для entrance animations при скролле
- `variants` для переиспользуемых анимаций
- Floating эффекты: `y: [0, -8, 0]` с `repeat: Infinity`

### Tailwind анимации:
- Базовые: `animate-fade-in`, `animate-scale-in`, etc.
- Кастомные keyframes в `index.css`

---

## 📱 RESPONSIVE BREAKPOINTS

Tailwind стандартные:
- `sm`: 640px
- `md`: 768px
- `lg`: 1024px
- `xl`: 1280px
- `2xl`: 1536px

**Подход**: Mobile-first (базовые стили для mobile, `md:`, `lg:` для desktop)

---

## 🔍 ПОИСК И НАВИГАЦИЯ ПО КОДУ

### Где искать компоненты:
- Все секции: `src/components/sections/`
- UI компоненты: `src/components/ui/`
- Утилиты: `src/lib/`
- Контексты: `src/contexts/`

### Где менять контент:
- Тексты секций: прямо в компонентах секций
- Контакты: `src/lib/contact.ts`
- Тарифы: `src/components/sections/ParticipationLevels.tsx`
- Формы: `src/components/sections/LeadForm.tsx` и другие формы

---

**Версия документа**: 2025-12-18  
**Последнее обновление**: После добавления EngineeringCore.tsx
