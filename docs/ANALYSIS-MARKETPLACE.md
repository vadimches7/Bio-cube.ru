# Анализ проекта Bio-Cube.ru для превращения в платформу-конструктор/маркетплейс

## 📊 Текущее состояние проекта

### Технологический стек
- **Frontend**: Vite + React 18 + TypeScript
- **UI Framework**: TailwindCSS + shadcn/ui (Radix UI)
- **Роутинг**: React Router v6
- **State Management**: React Context API
- **Forms**: React Hook Form + Zod
- **Backend Integration**: 
  - Supabase (клиент, но не используется активно)
  - Netlify Functions (lead-webhook)
- **Deployment**: Netlify
- **Build Tool**: Vite

### Архитектура приложения

#### Текущая структура
```
src/
├── pages/              # Страницы (Index, NotFound, Privacy, Offer)
├── components/         # Компоненты
│   ├── sections/      # Секции лендинга (20 компонентов)
│   └── ui/            # UI компоненты (shadcn/ui - 48 компонентов)
├── contexts/          # React Context (ServiceModeContext)
├── lib/              # Утилиты (contact, phone, submitLead, utils)
├── hooks/            # Кастомные хуки
└── integrations/     # Интеграции (Supabase)
```

#### Ключевые особенности
1. **Режимы работы** (`ServiceModeContext`):
   - `installation` - установка аквариумов
   - `service` - обслуживание
   - `decoration` - декор
   - Режимы влияют на контент секций через query-параметр `?mode=`

2. **Система форм**:
   - Единый helper `submitLead()` для всех форм
   - Защита от ботов (honeypot)
   - Rate limiting (60 сек)
   - UTM-трекинг
   - Интеграция с CRM через Netlify Function → Albato webhook

3. **Компонентная структура**:
   - Модульные секции (20+ компонентов)
   - Переиспользуемые UI компоненты (shadcn/ui)
   - Glass OS дизайн-система

---

## 🎯 Что нужно для превращения в маркетплейс

### 1. Многотенантность (Multi-tenancy)

#### Текущее состояние
- ❌ Жестко закодированный контент (BioCube)
- ❌ Нет системы пользователей/аккаунтов
- ❌ Нет разделения данных по клиентам

#### Что нужно добавить
- ✅ **Система аккаунтов**:
  - Регистрация/авторизация (Supabase Auth)
  - Роли: `admin`, `tenant`, `user`
  - Профили организаций
  
- ✅ **Изоляция данных**:
  - Каждый tenant имеет свой набор секций
  - Собственные настройки (логотип, цвета, контакты)
  - Собственные формы и интеграции

- ✅ **Домены/поддомены**:
  - `tenant1.bio-cube.ru` или `tenant1.biocube.ru`
  - Или через path: `/t/tenant1`

### 2. Конструктор страниц (Page Builder)

#### Текущее состояние
- ❌ Секции жестко закодированы в `Index.tsx`
- ❌ Контент в компонентах (hardcoded)
- ❌ Нет возможности редактировать через UI

#### Что нужно добавить
- ✅ **Система блоков (Blocks)**:
  - Базовые блоки: Hero, Services, Reviews, FAQ, Forms, etc.
  - Каждый блок - отдельный компонент с настройками
  - Drag & Drop редактор (react-beautiful-dnd или dnd-kit)
  
- ✅ **Конфигурация блоков**:
  ```typescript
  interface BlockConfig {
    id: string;
    type: 'hero' | 'services' | 'reviews' | 'faq' | ...;
    data: Record<string, any>; // Данные блока
    styles: Record<string, any>; // Стили блока
    order: number;
  }
  ```

- ✅ **Редактор контента**:
  - WYSIWYG для текстов
  - Загрузка изображений
  - Настройка цветов/шрифтов
  - Предпросмотр в реальном времени

### 3. База данных

#### Текущее состояние
- ❌ Нет структурированной БД
- ❌ Контент в коде
- ❌ Контакты в `lib/contact.ts`

#### Что нужно добавить
- ✅ **Supabase Database Schema**:
  ```sql
  -- Организации (tenants)
  organizations (
    id, name, slug, domain, logo_url, 
    theme_colors, settings, created_at
  )
  
  -- Пользователи
  users (
    id, email, role, organization_id, 
    profile_data, created_at
  )
  
  -- Страницы
  pages (
    id, organization_id, slug, title, 
    blocks (JSONB), seo_settings, published, created_at
  )
  
  -- Блоки
  blocks (
    id, page_id, type, config (JSONB), 
    order, created_at
  )
  
  -- Формы
  forms (
    id, organization_id, name, fields (JSONB), 
    webhook_url, created_at
  )
  
  -- Заявки
  leads (
    id, organization_id, form_id, data (JSONB), 
    utm_params, created_at
  )
  
  -- Медиа
  media (
    id, organization_id, url, type, 
    metadata, created_at
  )
  ```

### 4. Система аутентификации и авторизации

#### Текущее состояние
- ❌ Нет авторизации
- ❌ Supabase клиент есть, но не используется

#### Что нужно добавить
- ✅ **Supabase Auth**:
  - Email/password регистрация
  - OAuth (Google, GitHub)
  - Magic links
  - JWT токены
  
- ✅ **Row Level Security (RLS)**:
  - Политики доступа в Supabase
  - Изоляция данных по organization_id
  - Роли и права доступа

### 5. API и Backend

#### Текущее состояние
- ✅ Netlify Function для webhook
- ❌ Нет REST API
- ❌ Нет GraphQL

#### Что нужно добавить
- ✅ **Supabase Edge Functions**:
  - `/api/pages` - CRUD страниц
  - `/api/blocks` - CRUD блоков
  - `/api/forms` - CRUD форм
  - `/api/media` - загрузка файлов
  - `/api/organizations` - управление организациями

- ✅ **Netlify Functions** (или Supabase Functions):
  - Обработка форм
  - Интеграции с CRM
  - Webhooks

### 6. Файловое хранилище

#### Текущее состояние
- ❌ Статические файлы в `public/`
- ❌ Нет системы загрузки

#### Что нужно добавить
- ✅ **Supabase Storage**:
  - Бакеты по организациям
  - Загрузка изображений/видео
  - CDN для быстрой доставки
  - Оптимизация изображений

### 7. Админ-панель

#### Текущее состояние
- ❌ Нет админки

#### Что нужно добавить
- ✅ **Dashboard** (`/admin`):
  - Управление страницами
  - Редактор блоков (drag & drop)
  - Настройки организации
  - Управление формами
  - Аналитика (заявки, просмотры)
  - Управление пользователями

- ✅ **Редактор контента**:
  - Визуальный редактор страниц
  - Настройка блоков
  - Предпросмотр
  - Публикация

### 8. Система шаблонов

#### Текущее состояние
- ✅ Есть дизайн-система (Glass OS)
- ❌ Нет шаблонов для переиспользования

#### Что нужно добавить
- ✅ **Библиотека шаблонов**:
  - Готовые наборы блоков
  - Темы оформления
  - Примеры страниц
  - Marketplace шаблонов (платные/бесплатные)

### 9. Интеграции

#### Текущее состояние
- ✅ Интеграция с CRM через webhook (Albato)
- ❌ Нет других интеграций

#### Что нужно добавить
- ✅ **Система интеграций**:
  - CRM (AmoCRM, Битрикс24, etc.)
  - Email (SendGrid, Mailgun)
  - SMS (Twilio, Sms.ru)
  - Analytics (Google Analytics, Яндекс.Метрика)
  - Платежи (Stripe, ЮKassa)
  - Webhooks (кастомные)

### 10. Масштабируемость и производительность

#### Текущее состояние
- ✅ Vite для быстрой сборки
- ✅ Code splitting (manual chunks)
- ❌ Нет кеширования
- ❌ Нет CDN для статики

#### Что нужно добавить
- ✅ **Оптимизация**:
  - ISR (Incremental Static Regeneration)
  - Кеширование страниц
  - Lazy loading компонентов
  - Image optimization
  - CDN (Cloudflare, Netlify CDN)

---

## 🏗️ План миграции

### Фаза 1: Фундамент (2-3 недели)
1. ✅ Настройка Supabase проекта
2. ✅ Создание схемы БД
3. ✅ Настройка аутентификации
4. ✅ Базовая админ-панель
5. ✅ Система организаций (multi-tenancy)

### Фаза 2: Конструктор (3-4 недели)
1. ✅ Система блоков
2. ✅ Drag & Drop редактор
3. ✅ Редактор контента
4. ✅ Предпросмотр страниц
5. ✅ Публикация страниц

### Фаза 3: Интеграции (2-3 недели)
1. ✅ Система форм (динамические)
2. ✅ Интеграции с CRM
3. ✅ Загрузка медиа
4. ✅ Webhooks

### Фаза 4: Полировка (2-3 недели)
1. ✅ Шаблоны и темы
2. ✅ Аналитика
3. ✅ Оптимизация производительности
4. ✅ Документация

---

## 📦 Необходимые зависимости

### Новые зависимости
```json
{
  "dependencies": {
    "@dnd-kit/core": "^6.0.0",
    "@dnd-kit/sortable": "^7.0.0",
    "@dnd-kit/utilities": "^6.0.0",
    "@supabase/auth-helpers-react": "^0.4.0",
    "@supabase/storage-js": "^2.0.0",
    "react-dnd": "^16.0.0",
    "react-dnd-html5-backend": "^16.0.0",
    "react-quill": "^2.0.0", // WYSIWYG редактор
    "zod": "^3.25.76", // уже есть
    "react-hook-form": "^7.61.1", // уже есть
    "date-fns": "^3.6.0" // уже есть
  }
}
```

---

## 🔄 Рефакторинг текущего кода

### 1. Вынести контент из компонентов
- Создать `src/lib/content.ts` или использовать БД
- Секции должны получать данные через props

### 2. Абстрагировать блоки
- Превратить секции в переиспользуемые блоки
- Добавить конфигурацию через props

### 3. Унифицировать формы
- Создать систему динамических форм
- Конфигурация форм через JSON

### 4. Темы и стили
- Вынести цвета/стили в конфигурацию
- Поддержка кастомных тем

---

## 🎨 Архитектура маркетплейса

```
┌─────────────────────────────────────────┐
│         Public Pages (Tenant)            │
│  /t/:slug или tenant.domain.com         │
│  - Рендеринг страниц из БД              │
│  - Динамические блоки                   │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         Admin Dashboard                  │
│  /admin                                 │
│  - Управление страницами                │
│  - Редактор блоков                      │
│  - Настройки организации                 │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         API Layer                        │
│  Supabase Edge Functions                │
│  - CRUD операции                        │
│  - Авторизация                          │
│  - Валидация                            │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         Database (Supabase)             │
│  - PostgreSQL                            │
│  - Row Level Security                    │
│  - Realtime subscriptions                │
└─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────┐
│         Storage (Supabase)               │
│  - Медиа файлы                           │
│  - CDN                                   │
└─────────────────────────────────────────┘
```

---

## ⚠️ Потенциальные проблемы и решения

### 1. Производительность
**Проблема**: Рендеринг страниц из БД может быть медленным

**Решение**:
- ISR (Incremental Static Regeneration)
- Кеширование на уровне CDN
- Оптимизация запросов к БД
- Lazy loading блоков

### 2. Безопасность
**Проблема**: Multi-tenancy требует изоляции данных

**Решение**:
- Row Level Security в Supabase
- Валидация на уровне API
- Rate limiting
- CORS настройки

### 3. Масштабируемость
**Проблема**: Рост количества tenant'ов

**Решение**:
- Горизонтальное масштабирование
- Кеширование
- Оптимизация БД (индексы)
- CDN для статики

### 4. Стоимость
**Проблема**: Supabase может стать дорогим при росте

**Решение**:
- Мониторинг использования
- Оптимизация запросов
- Кеширование
- Возможность миграции на собственный сервер

---

## 📝 Следующие шаги

1. **Обсуждение требований**:
   - Какие функции критичны для MVP?
   - Какие интеграции нужны в первую очередь?
   - Какая модель монетизации?

2. **Прототип**:
   - Создать базовую структуру БД
   - Реализовать простой редактор
   - Протестировать multi-tenancy

3. **Итеративная разработка**:
   - Начать с MVP
   - Добавлять функции постепенно
   - Собирать обратную связь

---

## 📚 Полезные ресурсы

- [Supabase Documentation](https://supabase.com/docs)
- [React DnD Kit](https://dndkit.com/)
- [shadcn/ui](https://ui.shadcn.com/)
- [Vite Documentation](https://vitejs.dev/)

---

**Дата анализа**: 2025-01-XX
**Версия проекта**: текущая (лендинг)
**Цель**: Превращение в платформу-конструктор/маркетплейс
