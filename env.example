## Пример переменных окружения для локальной разработки (Vite)
## Скопируйте в `.env` и заполните значениями.
##
## ВАЖНО:
## - Файл `.env` в этом репозитории игнорируется (в .gitignore).
## - Vite прокидывает в клиент ТОЛЬКО переменные с префиксом `VITE_`.

# Supabase (нужно для отправки заявок через Edge Function и для клиента)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

## Webhook URL НЕ должен быть на клиенте.
## Настройте его как секрет в Supabase Function (Project Settings → Functions/Secrets):
##   ALBATO_WEBHOOK_URL=...
##
## Ниже — только fallback для dev (не рекомендуется для prod):
# VITE_ALBATO_WEBHOOK_URL=

